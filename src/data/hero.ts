/**
 * PLACEHOLDER COPY — replace with the real Nezbara hero messaging.
 * The Hero reads only this file, so wording can change without touching the component.
 */

/** One word/phrase per line, stacked. Lines alternate white / slate. */
export const headlineLines = ['IDEAS.', 'BUILD.', 'REALITY.']

export const primaryCta = { label: "Let's Connect", to: '/contact' }

/**
 * `href` is an in-page anchor: the Hero scrolls to it via Lenis when an element with that id
 * exists, and does nothing otherwise (the #services section isn't built yet).
 */
export const secondaryCta = { label: 'See what we do', href: '#services' }

/** Small label to the left of the client marquee. */
export const clientStripLabel = 'We worked for'
