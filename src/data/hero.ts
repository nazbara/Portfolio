/**
 * PLACEHOLDER COPY — replace with the real Nezbara hero messaging.
 * The Hero reads only this file, so wording can change without touching the component.
 * Every string below (headline, CTA labels, strip label) is placeholder text.
 */

/** One word/phrase per line, stacked. Lines alternate white / slate. */
export const headlineLines = ['IDEAS.', 'BUILD.', 'REALITY.']

/** PLACEHOLDER label. */
export const primaryCta = { label: 'Start a project', to: '/contact' }

/**
 * PLACEHOLDER label. `href` is an in-page anchor: the Hero scrolls to it via Lenis when an
 * element with that id exists (the Services section), and does nothing otherwise.
 */
export const secondaryCta = { label: 'Explore services', href: '#services' }

/** PLACEHOLDER label, shown to the left of the client marquee. */
export const clientStripLabel = 'Trusted by'
